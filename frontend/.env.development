# Development — used by `npm run dev` (Vite mode = development)
# Defaults to local backend. Change to your Render URL if testing against deployed backend.
VITE_API_URL=https://opendrive.onrender.com
